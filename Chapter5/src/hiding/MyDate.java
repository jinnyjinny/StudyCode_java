package hiding;

public class MyDate {
    private int day; // get,set 을 쓰지 못하는 경우 -> read only
    // get,set을 쓰는 이유 -> 매개변수가 보호됐을 때, 지역변수로 받아 쓸 수 있게 하는 방법
    // 변수들을 private으로 하고, public으로 생성자를 만들어서 쓰는 경우 -> 정보 은닉

}


